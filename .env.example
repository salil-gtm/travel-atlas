# Copy this file to `.env` and fill in your key.
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash-lite

# Seconds the agent sleeps before each Gemini call (free-tier rate limit guard).
# Bump up if you start hitting 429s.
THROTTLE_SECONDS=4
